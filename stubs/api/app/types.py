"""Shared type aliases."""

from typing import TypeAlias
from uuid import UUID

# Entity ID types
ProjectId: TypeAlias = UUID
LoanId: TypeAlias = UUID
ContactId: TypeAlias = UUID
DocumentId: TypeAlias = UUID
TaskId: TypeAlias = UUID
UserId: TypeAlias = UUID

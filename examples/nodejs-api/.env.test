# Test Environment Configuration
# Used exclusively for running automated tests

NODE_ENV=test

# Database Configuration (Test Database)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=connect2_test
DB_USER=connect_user
DB_PASSWORD=connect_dev_password

# AWS LocalStack Configuration (for integration tests)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_ENDPOINT_URL=http://localhost:4566

# S3 Configuration
S3_BUCKET=connect-documents-test

# SQS Configuration
SQS_DOCUMENT_PROCESSING_QUEUE=document-processing-test
SQS_NOTIFICATION_QUEUE=notifications-test

# SNS Configuration
SNS_TOPIC_ARN=arn:aws:sns:us-east-1:000000000000:project-updates-test

# Server Configuration
PORT=3001
API_BASE_PATH=/api/v1

# Test-specific settings
TEST_TIMEOUT=10000
SUPPRESS_LOGS=false
